// returns true if valid input
// returns false if invalid (characters not 0 or 1)
// elem - input element from the html
function isValid(elem){
	return true;	//remove
	
	if (elem.value=="")
	{
		return false;
	}
	else{
		//TODO: Check if input contains a char that is not 0 or 1
	}
	
}