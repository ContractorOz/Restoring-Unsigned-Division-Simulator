//negate num using 2's comple
function negate(num){
	var negative = ""
	var i=0
	var found_one=false
	//for (i=num.length-1; i>0; i--)
	//check if num[i] is 1, if it is first instance, found_one = true
	//negate all remaining num afterwards
	
	return negative
}